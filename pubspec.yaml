name: book_shop_bal
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.10.0


dependencies:
  flutter:
    sdk: flutter
  flutter_svg:
  cupertino_icons: ^1.0.8
  flutter_screenutil: ^5.9.3
  get_it: ^9.2.0
  # for dependency injection generation
  injectable: ^2.7.1+4
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  # help for code formatting
  flutter_lints: ^6.0.0

  # for generating assets
  flutter_gen_runner: ^5.12.0

  # for dependency injection generation
  injectable_generator: ^2.12.0
  
  # for code generation
  build_runner: ^2.10.4
  


flutter:
  uses-material-design: true
  assets:
    - assets/images/
   



